import React from 'react'
import { CommonSelect } from '../../common'

const Select = ({ label, id, input, children }) => 
  <CommonSelect label={label} id={id} input={input} children={children} />

export default Select